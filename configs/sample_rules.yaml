packs:
  - strict

rules:
  - env: JWT_SECRET
    required: true
    severity: critical

  - id: docker_latest_tag
    severity: warning
  - docker:
      latest_tag: false

  - production:
      no_localhost: true
